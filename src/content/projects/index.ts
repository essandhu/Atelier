import { placeholderProject } from '@/content/projects/placeholder';
import type { Project } from '@/content/projects/schemas';

const projects: Project[] = [placeholderProject];

export default projects;
